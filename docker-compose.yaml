services:
  nextjs:
    build:
      context: .
      dockerfile: dockerfile

    container_name: erikwelander.se
    restart: unless-stopped

    ports:
      - "3000:3000"

    environment:
      NODE_ENV: production
